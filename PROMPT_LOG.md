# Prompt Log
## ЛР10: Задание 5 (передача сложного JSON между Python и Go)
**Инструмент:** GPT-5.3.

### Промпт 1
**Промпт:** "сделай задание 5 средней сложности и все файлы для задания пусть будут в папке task5"

**Результат:** Создана папка `task5` с двумя сервисами: `go-service` (Gin, `POST /process`) и `python-service` (FastAPI, `POST /forward` -> HTTP-вызов Go). Добавлены модели сложного JSON, `sample_payload.json` и инструкция запуска в `task5/README.md`.

### Промпт 2
**Промпт:** "теперь напиши тесты для python и go, а также обнови .gitignore если то требуется"

**Результат:** Добавлены тесты для Go и Python, выполнены прогоны (`go test`, `pytest`), обновлен `.gitignore` под дополнительные артефакты.

### Промпт 3
**Промпт:** "нет, тесты должны быть внутри main файлов"

**Результат:** Отдельные тестовые файлы удалены, реализованы встроенные self-tests прямо в `task5/go-service/main.go` и `task5/python-service/main.py`. README обновлен под запуск тестов через переменные окружения/прямой запуск `main.py`.

### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: на Windows использовать `py -3` вместо `python` в командах запуска.
- Время: ~35-45 мин

---

## ЛР10: Задание 7 (graceful shutdown в Go и Python)
**Инструмент:** GPT-5.3.

### Промпт 1
**Промпт:** "теперь сделай задание 7 и все файлы для этого задания должны быть в папке task7.Также дополни .gitignore по необходимости"

**Результат:** Создана папка `task7` с сервисами `go-service` и `python-service`, реализован graceful shutdown по `SIGINT/SIGTERM`, добавлены долгие endpoint-ы `/work` и инструкция проверки в `task7/README.md`.

### Промпт 2
**Промпт:** "теперь добавь тесты для python и go внутри main файлов"

**Результат:** В `task7/go-service/main.go` и `task7/python-service/main.py` добавлены встроенные self-tests и режим запуска через `RUN_SELF_TESTS=1`. Проверка self-tests выполнена успешно.

### Итого
- Количество промптов: 2
- Что пришлось исправлять вручную: учтен warning FastAPI о `@app.on_event("shutdown")` (код работает, но API помечен как deprecated).
- Время: ~20-30 мин

---

## ЛР10: Задание 1 (простое API на Go/Gin)
**Инструмент:** GPT-5.3.

### Промпт 1
**Промпт:** "теперь создай простое API на GO(Gin) c 2-3 эндпоинтами и внутренними тестами.Всё файлы должны быть в папке task1.Также дополни .gitignore если потребуется"

**Результат:** Создана папка `task1` с `go.mod`, `main.go` и `README.md`. Реализованы endpoint-ы `GET /health`, `GET /hello/:name`, `POST /sum`; добавлены встроенные self-tests в `main.go` и подтвержден их успешный запуск.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: не потребовалось.
- Время: ~10-15 мин
